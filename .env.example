# Copy to .env (gitignored) and fill in. Read by `python -m blockudoku.train`
# when the config has `wandb.enabled: true`. Real environment variables win over this file.
WANDB_API_KEY=

# Optional: only used when the config's wandb.entity is "" (full.yaml sets it).
# WANDB_ENTITY=
